"""sonos-doctor — portable Sonos + network health diagnostics.

Stdlib-only. Read-only against the network. Degrades gracefully when
optional enrichment sources (UniFi controller) are unavailable.
"""
__version__ = "0.1.0"
